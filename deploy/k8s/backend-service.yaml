apiVersion: v1
kind: Service
metadata:
  name: sku-parser-api
  labels:
    app: sku-parser-api
spec:
  selector:
    app: sku-parser-api
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: ClusterIP
